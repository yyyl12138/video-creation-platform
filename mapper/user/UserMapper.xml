<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.huike.video.modules.user.mapper.UserMapper">

    <select id="countByUsername" resultType="java.lang.Long">
        select count(1)
        from users
        where username = #{username}
    </select>

    <select id="countByEmail" resultType="java.lang.Long">
        select count(1)
        from users
        where email = #{email}
    </select>

    <select id="selectByUsernameOrEmail" resultType="com.huike.video.modules.user.entity.User">
        select
            id,
            username,
            email,
            password_hash,
            avatar_url,
            phone,
            last_login_time,
            status,
            role_id,
            created_at,
            updated_at
        from users
        where username = #{account}
           or email = #{account}
        limit 1
    </select>

</mapper>
