# 管理员测试账号说明（SQL 创建）

本文档用于**单独说明管理员测试账号**的创建与登录验证方式。

## 账号信息

- **用户名**：`admin`
- **密码**：`123456`
- **邮箱**：`admin@example.com`
- **角色**：`ROLE_ADMIN`

## 创建方式（执行 SQL）

管理员账号由 SQL 脚本创建：

- **脚本路径**：`backend/sql/admin_user.sql`

### 1) 选择数据库并执行

在 MySQL 客户端中执行（将 `your_db_name` 替换为实际库名）：

```sql
USE your_db_name;
SOURCE backend/sql/admin_user.sql;
```

### 2) 脚本做了什么

- 若不存在 `username='admin'` 的用户，则插入管理员用户记录
- 为该用户绑定管理员角色：`ROLE_ADMIN`
- 初始化管理员钱包数据（若脚本包含）

> 说明：脚本使用 **BCrypt(123456)** 作为密码哈希，因此创建后可直接使用 `admin / 123456` 登录。

## 登录验证

- **接口**：`POST /api/v1/auth/login`
- **请求体**：

```json
{"username":"admin","password":"123456"}
```

- **成功返回**：响应体通常会包含 `token / refreshToken / expireIn / userInfo` 等字段（以实际接口定义为准）

### curl 示例

```bash
curl -X POST "http://localhost:8080/api/v1/auth/login" ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"admin\",\"password\":\"123456\"}"
```

## 常见问题

### 1) 执行 SQL 后仍然无法登录？

- 确认是否连对了数据库实例/库名
- 确认 `users` 表里确实存在 `username='admin'`
- 确认 `roles` 表里存在 `role_name='ROLE_ADMIN'`
- 如已存在同名用户，脚本不会覆盖原密码；需要先删除旧 `admin` 或手动更新密码哈希

